# Button.Text

A component to display the lable of buttons

## Usage

```tsx
import {Button} from "@/components";
import React from "react";

const HomeScreen = () => {
  return (
    <Button>
      <Button.Text title="Default" />
    </Button>
  );
};

export default HomeScreen;
```

## Props

### title

Type: `string`
